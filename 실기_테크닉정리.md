### 인덱스튜닝_일반튜닝
```
where 절 마지막에 무슨무슨코드라고 적힌 단어가 보이면 튜닝각인가 생각한다음 IN으로 나누거나 합칠생각
or 쓸 각이면 무지성으로 그냥 바로 use_concat(또는 no_expand) 추가 사용 고려하기
```

### 인덱스튜닝_rangescan 
```
|| 보이면 , 랑 () 사용해서 인덱스 찍어보고 얼추 맞으면 바로 사용
SELECT에 겁나 길게 쓰고 where에 짧게 IN 보이면 IN 수정해야되는거 고려
실행계획 수정하라는건 오라클피부과 블로그 sort쪽 참고하기
```

### 인덱스튜닝_입력형
```
- 테이블 2개면 대부분 PK가 반대컬럼 테이블로 지정.(드라이빙 테이블 반대편)
- 반드시 입력 +  데이터 하루 10만건 씩 누적 :  /*+ LEADING(반드시 입력 적힌 컬럼) USE_NL(반대 컬럼) */
- 반드시 입력 + 모두 입력해도 되는데, 앞 몇자리만 입력 : LIKE 바인드변수 || '%' 
- 반드시 입력 : 바인드변수(:)만 사용
- 전화번호가 Not NULL 컬럼일 때 : 전화번호=NVL(:PHONE_NO,전화번호) 
- 전화번호가 NULL 허용 컬럼일 때 :
1. :PHONE_NO IS NOT NULL --> 전화번호=:PHONE_NO
2. :PHONE_NO IS NULL --> 추가 조건 없음.
```

### 1. NL 조인 및 서브쿼리 (부분범위 처리)
```sql
A. 스칼라 서브쿼리 튜닝 (Conditional Join)
상황: 특정 조건일 때만 조인 테이블 데이터가 필요할 때.
핵심: CASE문을 통한 조인 시도 제어.

SELECT /*+ leading(m) use_nl(s) */ 
       m.고객명,
       (SELECT CASE WHEN m.유형 = 'A' THEN s.데이터 ELSE NULL END
        FROM   서브테이블 s
        WHERE  s.키 = m.키)
FROM   메인테이블 m;

B. View Pushed Predicate (집계 뷰 조인)
상황: GROUP BY가 포함된 서브쿼리와 NL 조인할 때.
명령어: no_merge, push_pred

SELECT /*+ leading(c) use_nl(t) */ 
       c.고객명, t.집계값
FROM   고객 c,
       (SELECT /*+ no_merge push_pred */ 
               고객번호, SUM(금액) 집계값
        FROM   거래
        GROUP BY 고객번호) t
WHERE  t.고객번호 = c.고객번호;

C. 분기 조인 (Union All)
상황: 작업구분코드에 따라 조인 대상 테이블이 달라질 때.

SELECT /*+ leading(a) use_nl(b) */ a.*, b.*
FROM   작업지시 a, 개통접수 b
WHERE  a.구분 = 'A' AND b.번호 = a.번호
UNION ALL
SELECT /*+ leading(a) use_nl(c) */ a.*, c.*
FROM   작업지시 a, 장애접수 c
WHERE  a.구분 = 'B' AND c.번호 = a.번호;
```
### 2. 해시 조인 (대량 데이터 처리)
```sql
A. Build Input 제어 (Swap Join Inputs)
상황: 조인되는 테이블 중 작은 쪽을 메모리에 올릴 때.
명령어: swap_join_inputs

SELECT /*+ leading(대량A 대량B 소량C) 
           use_hash(대량B) use_hash(소량C) 
           swap_join_inputs(소량C) */
       *
FROM   대량A, 대량B, 소량C
WHERE  ... ;

B. 선(先) 집계 후(後) 조인
상황: 1대다 관계에서 '다'측을 먼저 집계하여 데이터량을 줄이고 조인할 때.

SELECT /*+ leading(o) use_hash(p) */ 
       p.상품명, o.총량
FROM  (SELECT /*+ no_merge */ 상품코드, SUM(수량) 총량
       FROM   주문상품
       GROUP BY 상품코드) o, 
       상품 p
WHERE  p.상품코드 = o.상품코드;
```
### 3. Top-N 및 랜덤 액세스 최소화
```sql
A. Index Descending + Stopkey
상황: 최신순 일부 데이터만 추출할 때.

Index: [조건컬럼 + 정렬컬럼]

SELECT *
FROM  (SELECT /*+ index_desc(a 인덱스명) */ *
       FROM   테이블 a
       WHERE  상태 = '10'
       ORDER BY 등록일시 DESC)
WHERE  ROWNUM <= 10;

B. Index FFS 후 ROWID 조인
상황: 대량 집계 후 결과 건수가 적을 때 테이블 방문 최소화.

SELECT /*+ leading(o) use_nl(p) */ p.상품명, o.총합
FROM  (SELECT /*+ index_ffs(a) no_merge */ 상품코드, SUM(금액) 총합, MIN(ROWID) rid
       FROM   주문상품 a
       GROUP BY 상품코드) o, 
       상품 p
WHERE  p.ROWID = o.rid;
```
### 4. 이력(History) 관리 공식
```sql
A. 점 이력 (최신 데이터)
Logic: 상관 서브쿼리 내 ROWNUM <= 1

SELECT a.장비번호, 
       (SELECT b.상태코드
        FROM  (SELECT 상태코드 FROM 상태이력 ORDER BY 변경일 DESC, 순번 DESC) b
        WHERE  b.장비번호 = a.장비번호
        AND    ROWNUM <= 1) AS 현재상태
FROM   장비 a;
B. 선분 이력 (직전 데이터)
Logic: 현재 시작 시점 바로 전 초(Second) 데이터 검색.

SELECT a.장비번호, b.상태코드 AS 직전상태
FROM   장비 a, 상태이력 b
WHERE  b.장비번호 = a.장비번호
AND    b.유효시작일 < a.최종변경일
AND    b.유효종료일 >= a.최종변경일 - 1/(24*60*60);
```
