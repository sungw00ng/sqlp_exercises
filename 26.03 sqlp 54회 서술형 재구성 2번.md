```
저작권을 고려하여, 26.03 sqlp 원본 문제를 재구성하여
비슷한 감각으로 재구성하였음.
- IcyEngDB
```
```sql
-- 구성

DROP TABLE WOONG.T_SRC;
CREATE TABLE WOONG.T_SRC

( C1 NUMBER

, C2 VARCHAR2(10)

, C3 NUMBER

);

​

DROP TABLE WOONG.T_TGT;

CREATE TABLE WOONG.T_TGT

( C1 NUMBER

, C2 VARCHAR2(10)

, C3 NUMBER

);

​

CREATE INDEX WOONG.TGT_X1 ON WOONG.T_TGT(C1);

​

CREATE OR REPLACE PUBLIC SYNONYM T_SRC FOR WOONG.T_SRC;

CREATE OR REPLACE PUBLIC SYNONYM T_TGT FOR WOONG.T_TGT;

​

INSERT INTO T_SRC

SELECT ROWNUM C1, NULL C2, 100000-ROWNUM C3

FROM DUAL

CONNECT BY LEVEL <= 100000

;

​

COMMIT;
```
​
- 문제
```sql
T_SRC 테이블
  (  C1    NUMBER 
   , C2    VARCHAR2(10)
   , C3    NUMBER
   );

T_TGT 테이블

  (  C1    NUMBER 
   , C2    VARCHAR2(10)
   , C3    NUMBER
   );
인덱스 TGT_X1 : C1
​
- 아래 실행계획은 SQL을 튜닝한 결과의 실행계획입니다.
- 아래에서 제시된 실행계획과 같이 튜닝하시오.
-   - NOLOGGING 사용 금지
-   - PARALLEL 사용 금지
​

DELETE FROM T1;

COMMIT;

​

INSERT /*+ APPEND PARALLEL(T_TGT 2) */ INTO T_TGT
SELECT    C1, 'A', C3 FROM T_SRC
   UNION ALL 
SELECT    C1, 'B', C3 FROM T_SRC;

​

COMMIT;

​
```

- 튜닝 후 실행계획 <br>
<img width="829" height="256" alt="image (1)" src="https://github.com/user-attachments/assets/f16d1d53-ae74-4254-bb4a-d979ab311d00" /><br>​


```sql
​
-- 답안
TRUNCATE TABLE WOONG.T_TGT;

ALTER INDEX WOONG.TGT_X1 UNUSABLE;
ALTER SESSION ENABLE PARALLEL DML;

​

INSERT /*+ APPEND PARALLEL(T_TGT 2) */ ALL
      INTO     T_TGT (C1, C2, C3)    VALUES (C1, 'A', C3)

      INTO     T_TGT (C1, C2, C3)    VALUES (C1, 'B', C3)
SELECT /*+ PARALLEL(T_SRC 2) */ C1, C3
FROM T_SRC ;

​

ALTER INDEX WOONG.TGT_X1 REBUILD PARALLEL 2;
ALTER INDEX WOONG.TGT_X1 NOPARALLEL;

​
ALTER SESSION DISABLE PARALLEL DML;

​```
